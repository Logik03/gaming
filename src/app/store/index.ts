/* import { ActionReducerMap } from '@ngrx/store';

import { gameReducer, GamesState } from './games/reducer';
import { jackpotReducer, JackpotsState } from './jackpots/reducer';

export interface AppState {
  games: GamesState;
  jackpots: JackpotsState;
}

export const reducers: ActionReducerMap<AppState> = {
  games: gameReducer,
  jackpot: jackpotReducer,
}; */